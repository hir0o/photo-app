<%= form_for([@post, @comment], method: :post, remote: true, ) do |f| %>
  <%= f.text_field :content, placeholder: "コメントを残す", class: "post-comment",rows: "1" %>
  <%= f.submit class: "dn" %>
<% end %>